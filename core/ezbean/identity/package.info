对象身份接口定义：
通过预定义接口，定义对象的类型，在gear启动时、运行时，有不同的存在形式
一般情况下，单一对象只会对应一种身份接口

1、EzBean
常驻内存的单例对象，统一被BeanFinder管理，运行时以DynamicProxy的实例存在
gear启动时扫描这些对象，会基于注解，追加运行时、启动时的附加逻辑

2、EzComponent
gear启动时扫描这些对象，会基于注解，追加运行时、启动时的附加逻辑